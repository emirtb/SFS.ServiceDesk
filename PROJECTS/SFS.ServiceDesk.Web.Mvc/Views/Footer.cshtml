<script>
        $("header.main-header a.logo").html("");
        $(".sidebar .user-panel").hide();
        $(".sidebar-form").hide();
        $(".main-footer").hide();

        $(document).ready(function () {
            /*
        var sizeH = $(".content-wrapper").height();
        var topHeadSizeH = $(".nav-bar").height();*/
        var sizeHContent = $("section.sidebar").height();
        $(".wrapper-content").height(sizeHContent);
        {LOGO_TOP}
        
        var modalLeft = 100;
        var modalTop = 100;
        var modalWidth = 1000;
        $(document).ready(function () {
            $("a.sidebar-toggle").click();
            if ($("body").hasClass("sidebar-collapse")) {
                modalLeft = 0;
            } else {
                modalLeft = $(".main-sidebar").width();
            }
            
            modalTop = 0;
            $(".popuplink").attr("top", modalTop);
            $(".popuplink").attr("left", modalLeft);
        });
        $(".sidebar-toggle").on("click", function (e) {
            e.preventDefault();
            setTimeout(function () {
                var val = "1";
                if ($("body").hasClass("sidebar-collapse") == false) {
                    modalLeft = $(".main-sidebar").width();
                } else {
                    modalLeft = 0;
                }
                
                modalTop = 0;

                $(".popuplink").attr("top", modalTop);
                $(".popuplink").attr("left", modalLeft);

            }, 1000);
        });


    });
    
    // $(".main-footer").hide();
</script>

